# Extra Python dependencies for the CL2K poster maker (develop-only
# extension). Kept out of requirements.txt so that file stays identical to
# main; installed by the CL2K block in deploy/docker/Dockerfile and needed
# locally to run the cl2k tests.
psd-tools[composite]==1.18.0
Wand==0.7.2
# CPU ONNX runtime for the PP-OCRv4 DBNet text detector (models/ppocr_v4_det.onnx)
# — powers the polarity-agnostic "tighten to letters" anchor. Fail-soft: if the
# import or model load fails, tighten falls back to the colour-key. Needs cp313
# wheels for the python:3.13-slim image (>=1.20); the InferenceSession/run API is
# version-stable (verified locally against 1.19.2 on py3.9).
onnxruntime==1.28.0
# SVG clear-logo rasterization (renderer._read_logo_image). Wand's own SVG
# delegate is absent from the runtime image (ImageMagick reports "no decode
# delegate for image format 'SVG'"), which 500s any title whose best logo is an
# SVG — and select_logo prefers SVG. cairosvg (pure-Python, the same rasterizer
# the holiday borders use) only needs libcairo2, already present via librsvg2-2.
cairosvg==2.9.0
