# CL2K poster-maker deps (:full-image only); kept out of requirements.txt so it matches main.
# Not psd-tools[composite]: it adds scipy + scikit-image only for composite(), unused here.
psd-tools==1.19.0
Wand==0.7.2
# CPU ONNX runtime for the PP-OCRv4 text detector behind "tighten to letters" (fail-soft).
onnxruntime==1.29.0
# SVG logo rasterization (renderer._read_logo_image): the image's ImageMagick has
# no SVG delegate, so Wand 500s on SVG logos. Needs only libcairo2 (via librsvg2-2).
cairosvg==2.9.0
